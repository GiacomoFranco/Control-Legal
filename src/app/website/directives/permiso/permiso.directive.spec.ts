import { PermisoDirective } from './permiso.directive';

describe('PermisoDirective', () => {
  it('should create an instance', () => {
    const directive = new PermisoDirective();
    expect(directive).toBeTruthy();
  });
});
